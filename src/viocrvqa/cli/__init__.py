"""Command-line entry points.

    python -m viocrvqa.cli.train           fine-tune on a data dir
    python -m viocrvqa.cli.eval_baseline   score a split
    python -m viocrvqa.cli.sample          read predictions / compare prompts
    python -m viocrvqa.cli.infer           one-off inference on any image
    python -m viocrvqa.cli.merge_reports   merge sharded eval outputs
"""
